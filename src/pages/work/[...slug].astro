---
import type { CollectionEntry } from "astro:content";
import { getCollection, render } from "astro:content";
import BaseHead from "../../components/BaseHead.astro";
import Header from "../../components/Header.astro";
import Footer from "../../components/Footer.astro";

export async function getStaticPaths() {
  const workEntries = await getCollection("work");
  return workEntries.map((entry) => ({
    params: { slug: entry.id },
    props: { entry },
  }));
}

interface Props {
  entry: CollectionEntry<"work">;
}

const { entry } = Astro.props;
const { Content } = await render(entry);
const { title, description, heroImage, tags } = entry.data;
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead title={`${title} — Andre Ortiz`} description={description} />
  </head>
  <body style="background: #FAFAF8;">
    <Header />

    <main class="max-w-6xl mx-auto px-6 py-12">

      <!-- Breadcrumb -->
      <nav class="animate-fade-up mb-8">
        <a
          href="/work"
          class="inline-flex items-center gap-1 text-sm no-underline transition-colors duration-150"
          style="color: #6B6B64;"
          onmouseover="this.style.color='#FF5C35'"
          onmouseout="this.style.color='#6B6B64'"
        >
          ← Back to Work
        </a>
      </nav>

      <!-- Hero image -->
      {heroImage && (
        <div class="animate-fade-up-delay-1 rounded-2xl overflow-hidden mb-10" style="border: 1px solid #E8E6E1;">
          <img
            src={heroImage}
            alt={title}
            class="w-full object-cover"
            style="max-height: 500px;"
          />
        </div>
      )}

      <!-- Title + tags -->
      <div class="animate-fade-up-delay-2 max-w-3xl mb-10">
        {tags && (
          <div class="flex flex-wrap gap-1.5 mb-4">
            {tags.map((tag: string) => (
              <span class="ao-tag">{tag}</span>
            ))}
          </div>
        )}
        <h1 class="m-0 mb-4" style="font-family: 'Fraunces', serif; font-size: clamp(2.5rem, 5vw, 4rem); line-height: 1.1; color: #1A1A18;">
          {title}
        </h1>
        <p class="m-0 text-xl leading-relaxed" style="color: #6B6B64;">
          {description}
        </p>
      </div>

      <!-- Content -->
      <div class="animate-fade-up-delay-3 max-w-3xl ao-md-content-container">
        <Content />
      </div>

    </main>

    <Footer />
  </body>
</html>
