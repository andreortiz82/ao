---
import BaseHead from "../components/BaseHead.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import { SITE_TITLE, SITE_DESCRIPTION } from "../config";
import style from "../styles/home.module.css";
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
  </head>
  <body>
    <main>
      <Header title={SITE_TITLE} />
      <section class="padded-content">
        <div class={style["greeting"]}>
          <span class={style["dot-animate-pulse"]}></span>
          <span>Current Learning</span>
        </div>
        <h1>
          Product Design, Front-End Development, Design Systems, and AI
          Enthusiast based in Austin, TX.
        </h1>
      </section>

      <!-- <section class={style["work-section"]}>
        {
          [
            "Design Systems",
            "Analytics Dashboard",
            "Skillaboration",
            "Illustrations & Branding",
          ].map((work) => {
            return (
              <article class={style["work-block"]}>
                <div class={style["work-card"]}>
                  <h5>{work}</h5>
                </div>
              </article>
            );
          })
        }
      </section> -->
      <Footer />
    </main>
  </body>
</html>
