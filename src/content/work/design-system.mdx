---
title: "Apex Design System"
description: "A scalable, accessible component library used by 200+ engineers across 4 product teams."
pubDate: "2024-01-15"
heroImage: "/covers/4.png"
tags: ["Design Systems", "React", "Accessibility", "Figma"]
role: ["Design Systems Lead", "Front-end Development"]
team: ["3 designers", "2 front-end engineers"]
duration: "18 months"
summary: "Four product teams. Four codebases. Zero shared UI language. We built the infrastructure that brought them together."
solutions:
  - title: "Theme switcher"
    summary: "A single token layer powers four distinct themes — default, dark, and two brand variants — without touching component code. Switch contexts instantly and see how the system adapts."
  - title: "Component gallery"
    summary: "Every component in the system, live and interactive. Buttons, inputs, sliders, lists — each one accessible by default, documented, and ready to use."
impact:
  summary: "Eighteen months in, the numbers were clear. But the real outcome was a shared language — designers linking to components instead of writing specs, engineers extending the system instead of reinventing it."
  metrics:
    - value: "40%"
      label: "Reduction in design-to-dev handoff time"
    - value: "200+"
      label: "Engineers actively using the system"
    - value: "4"
      label: "Product teams on a single component library"
    - value: "94%"
      label: "Internal satisfaction score"
---

## The Problem

Four product teams. Four codebases. Zero shared UI language.

When I joined Apex, the inconsistency was obvious almost immediately — not just visually, but functionally. The marketing team's buttons were subtly different from the dashboard's. The onboarding flow used a homegrown modal while the settings page used a third-party one. Three separate implementations of a date picker, each with different keyboard behavior. And every sprint, designers were rebuilding the same components from scratch in Figma, handing them off, and watching engineers interpret them differently.

The real cost wasn't aesthetic. It was velocity. Each new feature required re-solving solved problems. Bug fixes landed in one place but not another. And as the company scaled toward its Series B, the four product teams were drifting further apart — not together.

Something had to change.

## The Approach

I'd built design systems before, and I'd seen the failure mode: a team locks themselves in a room, emerges six months later with a "system," and wonders why no one adopts it.

We started with an audit — not a Figma audit, a real one. We scraped every deployed UI across all four products, catalogued every component variant, and mapped the divergence. That shared picture of the problem was the foundation for everything that followed.

From there, we established three design principles: predictable over clever, accessible by default, and engineer-first documentation. We built in the open from day one. Storybook was our source of truth. Engineers were invited to contribute, comment, and critique. Weekly office hours — drop-in, no agenda — where any team could bring a design problem and we'd work it out together.

## Key Decisions

**Token-based theming.** Every color, spacing unit, and type size lived as a design token. This made it possible to ship four distinct themes — default, dark, and two brand variants — without touching component code.

**React + Figma parity.** Every component had to exist in both Figma and React with matching props. We kept them in sync with a token export pipeline that ran on every release.

**Versioned releases with changelogs.** Semantic versioning, automated changelogs, deprecation notices with migration guides. Teams could upgrade on their own schedule without fear.

**Accessibility baked in.** Every component was built with keyboard navigation, ARIA roles, and screen reader testing from the start.
